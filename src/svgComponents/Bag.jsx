import * as React from "react"

const Bag = (props) => (
  <svg
    width={2}
    height={2}
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
    {...props}
  >
    <circle cx={1} cy={1} r={1} fill="#172329" />
  </svg>
)

export default Bag
